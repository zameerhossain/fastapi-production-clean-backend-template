name: Code Check

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  code_check:
    name: Code Quality Checks
    runs-on: ubuntu-latest

    env:
      PIPENV_VENV_IN_PROJECT: "1"
      PIP_CACHE_DIR: ${{ github.workspace }}/.cache/pip

    steps:
      # Step 1: Checkout code (avoid archive)
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Step 2: Install tar (fix extraction error)
      - name: Install tar
        run: sudo apt-get update && sudo apt-get install -y tar

      # Step 3: Setup Python 3.12
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      # Step 4: Cache pipenv virtual environment and pip cache
      - name: Cache pipenv dependencies
        uses: actions/cache@v3
        with:
          path: |
            .venv
            .cache/pip
          key: ${{ runner.os }}-pipenv-${{ hashFiles('Pipfile.lock') }}

      # Step 5: Install system dependencies
      - name: Install system dependencies
        run: |
          apt-get update
          apt-get install -y curl build-essential git

      # Step 6: Upgrade pip & install pipenv
      - name: Install pipenv
        run: python -m pip install --upgrade pip pipenv

      # Step 7: Install project dependencies
      - name: Install project dependencies
        run: ./scripts/install.sh

      # Step 8: Run code checks via Makefile
      - name: Run code checks
        run: make check
